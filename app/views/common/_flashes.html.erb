<% if flash.any? %>
    <% user_facing_flashes.any do |key, value| %>
      <div class="alert alert-dismissable alert-<%= flash_class(key) %>" >
          <button class="close" date-dismiss="alert"> X </button>
          <%= value %>
      </div>
    <% end %>
<% end %>
